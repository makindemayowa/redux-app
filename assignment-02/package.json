{
  "name": "assignment-02",
  "version": "1.0.0",
  "description": "Frontend tech assignment 02",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "yarn start --prefix client",
    "build:client": "yarn run build --prefix client",
    "dev": "concurrently \"yarn run server\" \"yarn run client\"",
    "start": "yarn run build:client && node server.js"
  },
  "keywords": [
    "travix"
  ],
  "author": "Travix",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^6.2.0",
    "cors": "^2.8.5",
    "express": "^4.17.1"
  }
}
